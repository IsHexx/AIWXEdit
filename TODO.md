# TODO

## Branding / cleanup

- [ ] Decide whether to migrate/rename the theme CDN repo referenced by `src/infrastructure/storage/ThemeCatalogData.ts` (`REPO_BASE` currently points to `IsHexx/wdwx-themes`). If migrated, update `REPO_BASE` accordingly.
- [ ] Rename legacy UI/CSS prefix from `.wdwxedit-*` to `.aiwxedit-*` (including matching `className` strings and any custom event names) while keeping behavior unchanged.

